@model Product

<h3>@Model.Name Information</h3>

<table class="table">
    <thead>
        <tr>
            <th>Product ID</th>
            <th>Name</th>
            <th>Price</th>
            <th>Category ID</th>
            <th>On Sale</th>
            <th>Stock Level</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@Model.ProductId</td>
            <td>@Model.Name</td>
            <td>@Model.Price</td>
            <td>@Model.CategoryId</td>
            <td>@Model.OnSale</td>
            <td>@Model.StockLevel</td>
        </tr>
    </tbody>
</table>


    <a asp-controller="Product" asp-action="UpdateProduct" asp-route-id="@Model.ProductId" class="btn btn-warning">Update this Product</a>

@* <form asp-action="UpdateProduct" asp-controller="Product" method="post" class="d-inline">
    <input asp-for="ProductID" type="hidden" />
    <button type="submit" class="btn btn-warning">Update</button>
</form> *@
<button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
    Delete this product
</button>
<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Confirm Deletion</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete this product? This action cannot be undone.
            </div>
            <div class="modal-footer">
                <form asp-action="DeleteProduct" asp-controller="Product" method="post" class="d-inline">
                    <input asp-for="ProductId" type="hidden" />
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
            </div>
        </div>
    </div>
</div>
